// Package winsession starts processes as a user signed in to Windows, from the service that
// runs as SYSTEM in session 0: the ticket browser (Edge will not run as SYSTEM) and the tray.
package winsession
